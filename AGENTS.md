完成一个独立部分后使用 `scope: detail` 格式 Commit 并 Push。

代码修改必须在独立 worktree 中完成。合并前在主工作区持有共享锁，确认主工作区位于
`main`，切到待验证分支完成测试，再合并回 `main` 并清理临时 worktree/分支。

每个协议或构建改动都需要单元测试、真实上游源码 E2E；涉及 TDLib C++ 时还需要原生编译
和测试验证。
